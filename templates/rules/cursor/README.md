# Cursor Templates

> TODO: Adapt rules for Cursor (`.cursor/` structure)
> Planned for evnict-kit v0.2.0
>
> Cursor uses `.cursor/rules/` folder with `.mdc` file extension.
> Context file is `.cursorrules` at project root.
